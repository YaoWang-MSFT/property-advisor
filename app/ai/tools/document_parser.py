def parse_document(file_path: str) -> str:
    """Parse a document (PDF, DOC, etc.) and return its text content."""
    # TODO: Implement document parsing logic (e.g., using PyPDF2, python-docx)
    return f"Parsed content from {file_path}"
